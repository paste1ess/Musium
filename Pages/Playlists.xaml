<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Musium.Pages.Playlists"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Musium.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Musium.Controls"
    xmlns:models="using:Musium.Models"
    mc:Ignorable="d">

    <GridView 
        Margin="16" 
        Header="Playlists" 
        HeaderTemplate="{StaticResource StyledHeaderTemplate}"
        ItemsSource="{x:Bind AllPlaylists}"
        IsItemClickEnabled="True"
        ItemClick="GridView_ItemClick">
        <GridView.ItemTemplate>
            <DataTemplate x:DataType="models:Playlist">
                <controls:PlaylistItemControl Playlist="{x:Bind}"/>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</Page>
